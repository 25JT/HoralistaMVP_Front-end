---

---

<link
  href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;900&display=swap"
  rel="stylesheet"
/>
<link
  href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,0"
  rel="stylesheet"
/>

<main id="titulo" class="pt-20 bg-blue-50 w-screen h-screen">
  <body class="font-display bg-background-light text-gray-800">
    <main class="flex-1 px-4 sm:px-6 lg:px-10 py-8">
      <div class="mx-auto max-w-7xl">
        <div class="flex flex-wrap justify-between gap-4 items-center mb-6">
          <div class="flex min-w-72 flex-col gap-1">
            <h1
              id="nombreNegocio"
              class="text-blakc text-3xl font-black leading-tight tracking-[-0.033em]"
            >
              HOLA
            </h1>
            <p
              class="italic text-gray-500 text-base font-normal leading-normal"
            >
              Administra y visualiza las reservas de tu negocio.
            </p>
          </div>
          <!-- <button
            class="border-2 border-blue-500 text-black hover:text-white flex min-w-[84px] max-w-[480px] cursor-pointer items-center justify-center gap-2 overflow-hidden rounded-lg h-10 px-4 bg-primary text-sm font-bold leading-normal tracking-[0.015em]"
            onclick="location.href='PrincipalCliente'"
          >
            <span class="material-symbols-outlined text-base">add</span>
            <span class="truncate">Nueva Cita</span>
          </button> -->
        </div>
        <div class="rounded-xl border border-gray-200 bg-white p-4 md:p-6 mb-6">
          <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 items-end"
          >
            <div class="flex flex-col gap-2">
              <label class="text-sm font-medium text-gray-700" for="date-range"
                >Rango de Fechas</label
              >
              <div class="relative">
                <span
                  class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"
                  >calendar_today</span
                >
                <input
                  class="w-full h-10 pl-10 pr-4 rounded-lg border-gray-300 bg-blue-50 focus:ring-primary focus:border-primary"
                  id="date-range"
                  placeholder="Seleccionar fechas"
                  type="text"
                />
              </div>
            </div>
            <div class="flex flex-col gap-2">
              <label
                class="text-sm font-medium text-gray-700"
                for="status-filter">Estado</label
              >
              <div class="relative">
                <select
                  class="w-full h-10 px-3 rounded-lg border-gray-300 bg-blue-50 focus:ring-primary focus:border-primary appearance-none"
                  id="status-filter"
                >
                  <option>Todos</option>
                  <option>Pendiente</option>
                  <option>Confirmada</option>
                  <option>Cancelada</option>
                </select>
                <span
                  class="material-symbols-outlined pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-gray-400"
                  >expand_more</span
                >
              </div>
            </div>
            <div class="flex flex-col gap-2">
              <label class="text-sm font-medium text-gray-700" for="search"
                >Buscar</label
              >
              <div class="relative">
                <span
                  class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"
                  >search</span
                >
                <input
                  class="w-full h-10 pl-10 pr-4 rounded-lg border-gray-300 bg-blue-50 focus:ring-primary focus:border-primary"
                  id="search"
                  placeholder="Buscar cliente..."
                  type="text"
                />
              </div>
            </div>
            <button
              id="btn-aplicar-filtros"
              class="border-2 border-blue-500 text-black hover:text-white w-full lg:w-auto flex min-w-[84px] max-w-[480px] cursor-pointer items-center justify-center overflow-hidden rounded-lg h-10 px-4 bg-primary text-sm font-bold leading-normal tracking-[0.015em]"
            >
              <span class="truncate" id="filtrar">Aplicar Filtros</span>
            </button>
          </div>
        </div>
        <div id="loader" class="flex items-center justify-center h-full">
          <span
            class="loader align-center flex justify-center items-center h-full"
          ></span>
        </div>
        <!-- Tabla de citas -->
        <div class="overflow-x-auto rounded-xl border border-gray-200 bg-white">
          <table class="w-full min-w-[800px] text-sm text-left">
            <thead class="bg-gray-50">
              <tr>
                <th
                  class="px-6 py-3 font-medium text-gray-500 uppercase tracking-wider"
                  >Fecha</th
                >
                <th
                  class="px-6 py-3 font-medium text-gray-500 uppercase tracking-wider"
                  >Hora</th
                >
                <th
                  class="px-6 py-3 font-medium text-gray-500 uppercase tracking-wider"
                  >Cliente</th
                >
                <th
                  class="px-6 py-3 font-medium text-gray-500 uppercase tracking-wider"
                  >Servicio</th
                >
                <th
                  class="px-6 py-3 font-medium text-gray-500 uppercase tracking-wider"
                  >Mensaje</th
                >
                <th
                  class="px-6 py-3 font-medium text-gray-500 uppercase tracking-wider"
                  >Estado</th
                >
                <th
                  class="px-6 py-3 font-medium text-gray-500 uppercase tracking-wider text-right"
                  >Acciones</th
                >
              </tr>
            </thead>
            <tbody id="tabla-citas-body" class="divide-y divide-gray-200">
              <!-- Las filas se generarán dinámicamente con JavaScript -->
            </tbody>
          </table>
        </div>

        <!-- Paginación -->
        <div class="flex items-center justify-between mt-6 px-2">
          <span class="text-sm text-gray-600 italic" id="info-paginacion">
          </span>
          <div class="flex items-center gap-2">
            <button
              id="btn-anterior"
              class="inline-flex items-center justify-center h-9 w-9 rounded-lg border border-gray-300 bg-white text-gray-600 hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed"
              disabled=""
            >
              <span class="material-symbols-outlined text-xl">chevron_left</span
              >
            </button>
            <button
              id="btn-siguiente"
              class="inline-flex items-center justify-center h-9 w-9 rounded-lg border border-gray-300 bg-white text-gray-600 hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed"
            >
              <span class="material-symbols-outlined text-xl"
                >chevron_right</span
              >
            </button>
          </div>
        </div>
      </div>
    </main>
  </body>
</main>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.2s ease-out;
  }
</style>

<script src="../utils/BodyMenuNegocioJs.js"></script>
<script src="../assets/Animaciones/animaAgendada.js"></script>
